# Server port (requested)
PORT=7079

# Local dev: allow Vite dev server
CORS_ORIGIN=http://localhost:7078

# --- Keys ---
GROQ_API_KEY=put_groq_key_here
DEEPGRAM_API_KEY=put_deepgram_key_here

# Google Cloud Text-to-Speech:
# IMPORTANT: Billing must be enabled on your GCP project.
# Point this to your service account JSON key file (do NOT keep inside /var/www/html).
GOOGLE_APPLICATION_CREDENTIALS=/var/www/keys/google-tts.json

# --- Deepgram STT defaults ---
DG_MODEL=nova-3
DG_LANGUAGE=multi

# Deepgram price estimate (multilingual Nova-3 PAYG shown in your older demo; override if your plan differs)
DG_PRICE_PER_MIN_MULTI=0.0052

# Voices cache TTL (seconds)
VOICES_CACHE_TTL_SEC=3600
